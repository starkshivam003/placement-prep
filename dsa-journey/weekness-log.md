# Weakness Log

| Problem | Category | What Went Wrong | Status |
|---|---|---|---|
| LC3756 | Segment Tree (custom merge) | Needed a data structure I haven't learned yet — array-based approach is fundamentally O(n²) for this range-query problem. Revisit after Trees/Segment Tree in Striver. | PARKED |
| LC0125 | Two Pointers / ASCII ranges | Wrote `if` instead of `else if` for mutually exclusive cases — double-appended characters. | RESOLVED |
| LC0029 | Bit Manipulation | Typo: subtracted from wrong variable (`divs` instead of `divd`) — didn't dry-test before assuming logic was wrong. | RESOLVED |
| LC0347 | Bounded value domain | Tried to index array directly by value without checking bounds — value can be negative/large. | RESOLVED |
| LC0034 | Binary Search / bounds-check ordering + signed-unsigned compare | (1) Bound check placed AFTER array access in while-condition instead of before — heap-buffer-overflow, same "missing bounds check" family as LC0347. (2) Compared signed int against unsigned `nums.size()` directly — silent wraparound risk. Working solution passes but is O(n) worst case (all-same-element array); true O(log n) needs two biased binary searches instead of expand-outward. | RESOLVED (crash) / OPEN (optimal complexity) |
| LC0043 | Big-number arithmetic / digit-array construction | No built-in integer type holds a 200-digit number — couldn't independently derive the grade-school multiplication + carry-propagation approach or the pos_high/pos_low index formula; needed heavy structural guidance. Reproduced and verified the solution, made small independent choices (broader zero-check, ASCII +/-48 instead of `to_string`), but not an authentic independent solve. Rewrite from scratch, from memory, required before marking resolved. | OPEN |
| LC3867 | GCD algorithm choice + C++ syntax (nested functions, vector/sort API, signed types) | Multiple compounding issues, worked on since morning of Jul 16: (1) First gcd attempt used trial division from x=2 upward with a `static int x` instead of Euclid's algorithm (gcd(a,b)=gcd(b,a%b)) — static var persists across calls, and trial division never terminates for coprime pairs, infinite recursion. (2) Attempted to nest a full function definition (gcd) inside another function (gcdSum) — not legal in C++. (3) In the Euclid's-formula retry, wrote `gcd(b,b%a)` instead of `gcd(b,a%b)` — swapped operand order. (4) `long long vector<int> A;` — invalid mixed-type declaration. (5) Called `A.sort(A.begin(),A.end())` as if sort were a vector method — it's a free function from `<algorithm>`. (6) Missing semicolon after a push_back call. (7) Correctly self-flagged that an O(n²) sort would TLE against n<=1e5 — still needs an O(n log n) sort written by hand (own logic, not std::sort per stated preference). | RESOLVED |
| LC0025 | Linked List (Hard) / operator typo `==` vs `=` | Second Hard problem ever, built entirely independently over ~8 hours (length count, head-insertion reversal, multi-group chaining with q/r/s/p pointers) — real structural win. Two bugs needed diagnosis, not full solves: (1) An earlier version had an off-by-one in the length-counting loop (started `l` at 1 instead of 0), which desynced the `while(l>=k)` group-count check from the list's real remaining length, causing a null dereference past the last node. (2) Wrote `head->next==NULL;` (comparison) instead of `head->next=NULL;` (assignment) — a no-op statement that silently failed to sever a stale pointer, leaving the reversed list connected back into a cycle, which caused a heap-use-after-free when the judge tried to traverse/free the result. Both bugs were syntactically valid, which is why dry-testing on paper missed them — the eye reads intent, not literal syntax, especially for `==` vs `=`. Compiling with `-Wall` would likely catch the second one directly. | RESOLVED |
| LC1260 | 2D Array / in-place value preservation | (1) First attempt: introduced a `temp` variable correctly intending to preserve an overwritten value before shifting, but never wrote it back into the array — the value was computed and discarded, not carried forward. (2) Off-by-one at the grid boundary: `grid[i+1][0]=grid[i][n-1]` on the last row (`i=m-1`) accessed `grid[m][0]`, out of bounds, since valid rows are `0..m-1`. Self-corrected both independently after a nudge toward where to look — dry-tested and rewrote using a proper `tail`/`head` running-swap pattern that correctly threads the overwritten value through the full single-pass traversal, and added `k %= (m*n)` on own initiative after checking whether it was actually needed against the given constraints (concluded it wasn't required for correctness/performance here, but added for robustness anyway). Verified correct against 4 test cases including a full-cycle k and a single-row grid. | RESOLVED |
| LC0033 | Binary Search on Rotated Sorted Array / condition logic (misplaced else, comparing against moving pointers instead of fixed ones) | Multiple attempts across sessions: (1) First correct solution found the answer but scanned for the rotation pivot in O(n), not O(log n) — a working but suboptimal solve. (2) Attempted the O(log n) reframe using two if statements intended as if/else-if, but the second else bound to the wrong if; also compared nums[mid] against nums[l], a moving pointer, instead of a fixed reference — caused early loop exit and missed the target entirely on [1,3], target=1. (3) Refined further with added conditions but logic still didn't converge; correctly self-diagnosed being at a wall from fatigue rather than push through. Final working O(log n) solution (verified: 200,000 random tests pass, ~20 steps for n=1,000,000, matching log₂(n)) uses the standard "one half is always cleanly sorted" reframe — determine which half of the current window has no rotation break by comparing nums[l] to nums[mid], then binary-search normally within whichever half is guaranteed sorted. | RESOLVED (with heavy structural guidance on the final approach — own attempts got close to the right shape across multiple tries first) |
| LC0082 | Linked List / dangling pointer after `delete` (use-after-free) | Solved independently in ~1.5 hours, own case analysis, own constraints-based sentinel value (`x=-102`, exploiting `abs(val)<=100` to pick a value guaranteed never to match a real node). Self-corrected after failed test cases. Separately investigated why `delete p` couldn't be used: traced that `q` was initialized equal to `head`, so when the very first node was itself a duplicate, `p` and `head` pointed to the same node — `delete p` freed that memory, but the `head` pointer variable itself still held the now-dangling address and was dereferenced later (`head->val==y`), causing a heap-use-after-free. Confirmed via AddressSanitizer. Root cause: freeing a node without accounting for every other pointer variable (here, `head`) that might still reference the same address afterward. | RESOLVED (correctness) / OPEN (safe `delete` restructuring not yet attempted) |
| LC0104 | Conceptual: tail recursion vs. branching recursion | Solution itself was correct on first attempt (O(n) time, O(n) space via call stack depth, correctly self-identified). Misidentified the recursion as tail recursion (assumed any recursion could trivially become a `while` loop). Clarified: tail recursion requires the recursive call to be the last action with nothing left to do after it returns, and only one recursive call per step (a linear chain) — this problem has two recursive calls plus post-call work (`max` + `1`), making it branching recursion, which requires an explicit stack (not a simple loop) to convert to iterative form. Deliberately deferred writing the iterative/explicit-stack version — correctly reasoned it's a separate, meatier exercise not needed while still building basic tree familiarity via Easy problems. | RESOLVED (concept) / DEFERRED (iterative version, by choice) |
| LC0628 | Maximum Product of Three Numbers / comparison-proxy fallacy | Correctly derived the hard part independently on first attempt — single-pass top-3/bottom-2 tracking via a shift-chain (`maxi[]`/`mini[]`), including proper handling of duplicate values. Took 6 attempts and ~3 hours because the final decision step compared a proxy (`mini[0]*mini[1]` vs `maxi[0]*maxi[1]`) instead of the two actual final candidates — the proxy isn't mathematically equivalent to comparing the real three-number products, since the two candidates don't share the same non-`maxi[0]` factors. Verified via a concrete counterexample (comparison predicted the wrong branch on a large random array). Fixed by computing both real candidates directly (`mini[0]*mini[1]*maxi[0]` vs `maxi[0]*maxi[1]*maxi[2]`, cast to `long long` to avoid overflow) and taking the max — no proxy needed, since both candidates were cheap to compute directly. This also made the separate `maxi[0]<0` special-case branch unnecessary and removable. General rule identified: don't build a shortcut comparison between two things unless it's proven equivalent to comparing the real final candidates — if the real values are cheap to compute, just compute them. | RESOLVED |
| LC3517 | Smallest Palindromic Rearrangement I / C-string vs std::string conventions | Core algorithm (frequency count + two-pointer pair placement) correct and optimal on first real attempt — O(n), which is the theoretical floor since every character must be read at least once; the 26-iteration outer loop is a constant, not a real complexity cost. Bug found during verification: constructed `sub` with size `n+1` and manually set `sub[n]='\0'`, treating `std::string` like a C-style char array — but `std::string` tracks its own length internally and never needs a null terminator; writing `'\0'` just added a real, counted extra character, making the returned string length `n+1` instead of `n`. Fixed by constructing `sub` at exactly size `n` and removing the terminator line. Root cause: translating Abdul Bari's C-style string habits directly into C++ without adjusting for std::string's different internal model. (Initially flagged an "all-odd-counts" edge case as open, but confirmed moot — the problem guarantees `s` is already palindromic, so that input shape can never actually occur.) | RESOLVED |
| LC3518 | Smallest Palindromic Rearrangement II / k-th permutation with duplicate letters | Correctly derived the core reduction independently (only need to solve for the first half of the palindrome, since the second half and middle character are forced) and identified the right general technique to reach for (position-by-position construction using arrangement counts instead of generating/storing permutations). Did not yet internalize the exact mechanics of computing a capped multinomial coefficient (avoiding real factorial overflow by capping growth once it exceeds a safe bound like 2×10^6, since only "≥k or not" matters) or the subtract-and-recurse logic for skipping past letters whose block doesn't contain k. Given a full working, verified solution and a simplified standalone version (plain k-th permutation of a multiset, no palindrome wrapper) to study and internalize before attempting to write it independently. Deliberately paused rather than force it through on day 5 of a daily-question streak. | OPEN |
| LC0080 | Remove Duplicates from Sorted Array II / in-place compaction, overcomplicated approach | Counting logic (first pass, tracking `k`) was correct and clean. For the actual in-place modification, reached for a "search forward/backward for a replacement element" approach with nested loops and a `seen` boolean, rather than the standard read/write two-pointer pattern — also had a syntax bug (`nums[j!=nums[i]]`, a boolean used as an array index instead of `nums[j]!=nums[i]` as a condition). After ~1 hour, given the standard solution directly: single forward pass, `write` pointer advances only when keeping an element, decision made by comparing `nums[i]` against `nums[write-2]` (two positions back in the *compacted* result, not the original array) — this single comparison replaces the need for a `seen` flag or any searching entirely. | RESOLVED (given, not self-derived) |
| LC0189 | Rotate Array / cyclic replacement using GCD | Solved via three of the standard approaches independently: (1) extra array (O(n) time, O(n) space), (2) naive repeated single-step shift (O(n×k) time, O(1) space — correctly self-diagnosed as TLE-prone), (3) three-reversal trick (O(n) time, O(1) space), all self-derived and verified correct, including edge cases (k=0, single-element array, k > n). The fourth/true "cyclic replacement" approach — moving each element directly to its final position in one pass, using gcd(n,k) to determine the number of independent cycles needed to cover every index — was understood conceptually (why gcd(n,k) gives the cycle count, why a chain following +k jumps must loop back to its start) but not independently implemented; the save-before-overwrite mechanic needed to do the swap chain in-place, and the do-while structure needed to place the first element before checking the loop-closing condition, were given directly rather than self-derived. | PARKED |
| LC0203 | Remove Linked List Elements / trailing-pointer pattern for "previous node" | Solved independently, verified against 8 test cases (all-matching, consecutive matches at head, single element, no matches, empty). Initially believed "going back" to the previous node was needed and impossible in a singly linked list — resolved by realizing a second pointer (`q`) kept permanently one step behind `p`, advanced in lockstep from the start, achieves the same effect without ever traversing backward. Named as a reusable pattern: whenever a singly-linked-list problem seems to need "the previous node," keep a trailing pointer from the start rather than looking for a way to go back — same underlying idea as the fast/slow pointer work in hasCycle and reverseKGroup. | RESOLVED |
| LC0328 | Odd Even Linked List / unnecessarily complex parallel-pointer logic | Solved independently and correct, verified against 7 test cases (odd/even length lists, single element, empty, already-short lists). Noticeably more branch-heavy than necessary — multiple nested `if`/`break` pairs required to keep the two interleaved chains (odd-indexed, even-indexed) in sync while advancing. Flagged for a later simplification pass rather than fixed immediately; same category as the earlier swapPairs duplication catch — heavy branching in working code is a signal a cleaner unified loop structure likely exists underneath. | OPEN (simplification) |
| LC0234 | Palindrome Linked List / O(1) space follow-up | Solved correctly with O(n) time, O(n) space using a self-built stack (vector<int> + top index), after fixing a real bug (push() writing to stk[top] via operator[] on an empty vector instead of stk.push_back(val), which segfaulted — operator[] never grows a vector, only push_back does). Recognized that the O(n)/O(1) follow-up requires combining three techniques already built independently in earlier problems: fast/slow pointer to find the list's middle (from hasCycle), in-place reversal of the second half (from reverseKGroup), then walking two pointers forward together to compare halves (from removeElements' trailing-pointer pattern). Noticed, while reasoning through this, that plain single-pass linked list reversal (the basic Easy version, not the k-group version) has never actually been solved as its own standalone problem — reverseKGroup was attempted and solved directly without first building the simpler base case. Parked both: the O(1) space palindrome solution, and — more importantly — going back to solve plain Reverse Linked List (LC0206) from scratch as a foundational gap-fill before returning to this. | RESOLVED |
| LC2996 Missing Integer | STL / Constraint-Safety | Solved correctly with a hand-rolled `B[51]` fixed array — worked, but correctness silently depended on an unwritten constraint (`nums[i]<=50`), and array sizing was arrived at reactively across 7 failed attempts instead of derived up front. Better version uses `unordered_set` (STL, not yet learned) for a constraint-independent membership check. | PARKED (rewrite independently once `unordered_set`/STL is actually learned, not just shown) |
